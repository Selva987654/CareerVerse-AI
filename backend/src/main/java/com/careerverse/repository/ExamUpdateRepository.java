package com.careerverse.repository;

import com.careerverse.model.ExamUpdate;
import org.springframework.data.jpa.repository.JpaRepository;

public interface ExamUpdateRepository extends JpaRepository<ExamUpdate, Long> {
}
